# 织流 Loom

> 本地运行的智能体流水线工作台：把一件事拆成可编辑的多步骤流程，交给本机已登录的
> **Claude Code / Codex CLI** 逐步执行，产物实时落在你自己的磁盘上。

下载：https://lxlrwxs.top/modelflow/ ｜ 需要 Windows 10/11 ｜ 完全本地运行，无账号、无遥测

---

## 它解决什么

数学建模、调研报告、长文档写作这类任务，难点不在"让 AI 写一段"，而在**一整条链路**：
需求要钉死、方案要拆、资料要收集、逐项要执行、写完要自检、改完要交付。

Loom 把这条链路做成**可编辑的流程**：

- **编排流程**：可视化增删步骤，每步绑定技能（skill）、产物文件名、角色（执行/复核/编辑）、
  引擎与模型，并可设检查点暂停等确认。
- **技能库**：每步的提示词规范。支持新建、编辑、另存副本，以及导入标准 skill 包（zip / SKILL.md）。
- **运行**：SSE 实时流式执行 —— 转录区逐步显示工具调用与输出，右上角进程卡对应每一步状态，
  工作区文件面板实时列出智能体正在写的文件（markdown 就地渲染、图片/PDF 内联预览）。
- **对话式局部修订**：跑完不满意，不用重来 —— 指着某一步说怎么改，只重写那一步。
- **流程导入导出**：一条流程 = 一个 JSON 文件，可分享、可备份、可导回。

## 一条硬规则

**正文只能由本机 CLI 智能体产出。** 设置里的 API 预设只用于把端点/密钥/模型注入给
Claude Code 或 Codex CLI，Loom 自己不会直连任何大模型接口生成内容。
所以用之前请先装好并登录其中一个：

- [Claude Code](https://docs.anthropic.com/claude/docs/claude-code)（`claude` 命令可用）
- [Codex CLI](https://github.com/openai/codex)（`codex` 命令可用）

## 从源码运行

```bash
pip install -r requirements.txt
python run.py                 # http://127.0.0.1:8000
python run.py --port 8001     # 换端口
python run.py --auto-kill     # 端口被旧进程占用时自动释放
```

回归测试（122 条，不需要网络）：

```bash
python -m pytest -q
```

## 目录结构

```
app/            FastAPI 后端
  main.py         路由与全部 /api
  runner.py       流程执行引擎：检查点、SSE 事件流、工作区、产物
  agents.py       Claude / Codex CLI 适配层（参数拼装 + 流式解析）
  presets_library.py  内置流程与技能模板（可恢复出厂）
  updater.py      读 latest.json 检查更新、下载安装包、核对 sha256
static/         前端（原生 HTML/CSS/JS，无框架、无构建步骤）
skills/         随包内置技能（只读；用户自建技能落在数据目录）
tests/          pytest 回归，含静态资产契约测试
loom_launch.py    打包态入口（pywebview 原生窗口，失败则退回浏览器）
run.py            源码态入口
make_release.py   PyInstaller onedir + Inno Setup → release/Loom-<版本>-setup.exe
upload_cos.py     上传安装包与 latest.json 到腾讯云 COS
```

数据目录：源码运行在 `modex-data/`，安装后在 exe 同级 `data/`
（SQLite + 工作区 + 自建技能 + 导出件）。升级不覆盖它，卸载也不删它。

## 打包与发布

```bash
python make_icon.py                                  # 改过图标才需要
python make_release.py --version 1.0.1 --notes "…"   # 产出 setup.exe + latest.json
python upload_cos.py                                 # 传 COS（需要本地密钥文件，不入库）
```

安装包只带应用本体和依赖，**不会把开发机的 `modex-data/` 打进去**
（`make_release.py` 里有显式检查）。

## 隐私

没有服务端账号体系，没有埋点，不上传任何内容。你的流程、提示词、产物全部留在本机。
唯一的对外请求是：检查更新时读一次版本清单，以及智能体 CLI 自己与模型服务通信。

---

## English

Loom is a **local agent pipeline workbench**. You compose a multi-step workflow
(each step bound to a skill, an output file, a role and an engine), then run it
against the **Claude Code or Codex CLI already installed and signed in on your
machine**. Content is never generated by the app itself — API presets only get
injected into those CLIs.

Runs stream over SSE: a transcript of tool calls, a step-status card, and a live
workspace panel that shows the files the agent is writing (markdown rendered,
images and PDFs inline). Finished steps can be revised conversationally without
rerunning the whole pipeline. Workflows export to and import from a single JSON file.

Everything stays on disk in a local data folder. No accounts, no telemetry.

```bash
pip install -r requirements.txt && python run.py     # http://127.0.0.1:8000
python -m pytest -q                                   # 122 regression tests
```
