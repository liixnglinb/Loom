# 图表选型三阶段（Chart Select Guide）

> 给 AI 在写数据图脚本**之前**执行的强制流程。目的：先选对图，再画好图；避免"脚本写得漂亮但图型不匹配"。
> 配套文件：chart_library.md（52 类图：名字 + 适配判定 + 画法出处）。

## 总则：每个待画的数据结果，先走三步，再动笔

```
Step 1  数据判定   → 定"类别"（不看任何图）
Step 2  候选 3 个 → 只看 chart_library 的名字列，选 3 个
Step 3  终选 1 个  → 排序打分 + 写一句选择理由 → 打开 recipe 出图
```

---

## Step 1 · 数据判定（先不看图）

按「数据形态 + 论文想回答的问题」定类别：

| 数据形态 / 要回答的问题 | 类别 |
|---|---|
| 几组方法/方案在指标上的数值对比、谁更好 | 比较类 |
| 随时间/迭代/序列变化的走势、收敛、预测 | 趋势类 |
| 数值的分散程度、分布形态、组间分布差异 | 分布类 |
| 两个（或多个）变量的关系、相关、回归拟合 | 相关/回归类 |
| 部分占整体、多维综合评估、降维展示 | 组成/多维类 |
| 生存/专业统计/机器学习诊断/时空 | 专业/高级类 |
| 流程/结构/路线（非数据） | 流程/架构图（HTML 引擎） |

**判定硬规则**（数据没到就不画）：
- 只有 1 个数值列 → 不做"比较类"，考虑分布或汇总表
- 没有时间/序列维度 → 不做"趋势类"
- 样本量 <30 且不做推断 → 不做密度/箱线，散点/柱状更诚实
- 缺失/NaN 未处理 → 先补先清，不画残缺图

## Step 2 · 候选 3 个（只看名字）

- 打开 `chart_library.md`，只读目标类别下的**图名 + 适配判定**列（不读 recipe 原文）。
- 依据：组数、样本量、是否要误差棒、是否有显著性、类别是否多（决定水平/垂直/棒棒糖 vs 柱状等细节取向）。
- 列出恰好 3 个候选（写进脚本注释：`候选: A / B / C`）。

**候选时的取舍提示**：
- 类别名长 / 类目 ≥6 → 柱状改 水平条形/棒棒糖
- 需要同时看分布形状与统计量 → 小提琴/雨云 > 箱线
- 散点互相遮挡 → 降级 二维密度/热力
- 数据精确、组少 → 优先带误差棒/置信区间的图（点误差/分组柱状），不要用示意性强的图

## Step 3 · 终选 1 个（打分 + 理由）

从 3 个候选中按权重排序选 1 个：

| 维度 | 权重 | 判定 |
|---|---|---|
| 适配度 | 0.45 | 是否精确匹配数据形态与要回答的问题（硬门槛，不合则直接淘汰） |
| 可读性 | 0.35 | 读者 3 秒能否看懂；信息密度是否刚好（别为"高级"牺牲清晰） |
| 高级感 | 0.20 | 只在前两个都达标时充当加分项（渐变/配色/留白精致度） |

**规则**：
1. 适配度不过关直接淘汰，即使很漂亮。
2. 高级感不是否决项：清晰朴素 > 花哨难懂。
3. 必须写一句选择理由，格式固定，写进脚本头注释：
   `选图理由：<选中的图名> —— <数据形态一句话>，需展示<要点>，<另一个候选>因<原因>不选。`
   （理由会进工作区日志/论文过程，供追溯为什么用这张图。）
4. **只画终选的 1 张**，不要把 3 个候选都画出来。

## 执行落地

- 本指南 + chart_library.md 已随 `_utils/` 注入工作区（与 figure_recipes_*.md 同一机制），可直接 `cat _utils/chart_select_guide.md` / `cat _utils/chart_library.md`。
- 每个 `gen_fig_*.py` 脚本头注释必须包含：`候选: ...` 与 `选图理由：...` 两行。
- 抄 recipe 时注意 `figsize` 按长宽比档位换算（见 chart_library 顶部说明），不要照抄 recipe 占位值。
- 视觉质检（若开启）会在 script 输出图上复读该理由，跑偏时提示重选。